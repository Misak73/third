Алгоритм нахождение _пропущенных чисел
начало
	ввод(k);
	вывод('введите массив последовательных элементов от 1 до k, пропустив 2 из них:');
	для всех элементов массива
		ввод(массив);
	вывод(массив);
	для всех элементов массива
		Если элемент_массива+1 <> элемент_массива + 1 то
			вывод(элемент_массива+1);
конец.